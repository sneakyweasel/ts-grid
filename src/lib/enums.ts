export const enum TileType {
    Triangle,
    Square,
    Hexagon,
}

export const enum EdgeType {
    Edge,
    Vertex,
    Tile,
}

export const enum Direction {
    North,
    South,
    East,
    West,
}